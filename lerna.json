{
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "npmClient": "yarn",
  "publish": {
    "ignoreChanges": ["ignored-file", "*.md"],
    "message": "chore(release) owen: publish"
  }
}
